# Dependencias
node_modules/

# Build y archivos temporales
dist/
.vite/

# Variables de entorno con credenciales — NUNCA subir al repo
.env
.env.development
.env.production
# .env.example SÍ se commitea — es el template sin credenciales para nuevos devs